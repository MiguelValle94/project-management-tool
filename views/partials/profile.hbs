<img src="{{user.avatar}}" class="w-100 rounded-circle mb-3" style="width:60px!important" />

<p>
  {{user.name}}<br>
  Username: <a href="/{{user.username}}">{{user.username}}</a>
</p>

<form action="/logout" method="post">
  <button type="submit" class="btn btn-outline-danger btn-sm">Logout</button>
</form>